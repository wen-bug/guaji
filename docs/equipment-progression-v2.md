# 装备成长 V2

## 状态摘要

当前实现使用五阶装备、六个槽位基础模板、实例五行原型、随机基础属性、手动强化分配和随机战斗词条。装备没有角色等级穿戴要求；炼器不需要图纸解锁。

事实来源：

- 核心装备索引：[index.tres](../resources/equipment/index.tres)
- 十四件装备的数值配置：[resources/equipment/](../resources/equipment/)
- 统一加载与校验：[equipment_config_parser.gd](../scripts/game/data/equipment_config_parser.gd)
- 存档与操作接口：[game_state.gd](../scripts/game/core/game_state.gd)
- 战斗结算：[combat_skill_executor.gd](../scripts/game/combat/combat_skill_executor.gd)

## 六槽位模板

运行时核心注册表只包含 `weapon`、`helmet`、`armor`、`leggings`、`gloves`、`accessory` 六个槽位 ID。资源层使用十四件独立装备配置；随机炼器和敌人掉落按 `index.tres` 的槽位权重抽取，当前六槽位均为 1。定向打造只选择槽位，不能指定五行原型或随机属性。

武器和饰品生成时按各自资源的 `selection_weight` 抽取一个五行原型，当前十种原型权重均为 1。旧五行模板 ID 由索引解析为通用槽位和具体装备，不进入打造、掉落或调试模板列表。

装备实例保存 `equipment_variant_id`、`equipment_base_name`、`rolled_attribute_stats` 和最终 `base_attributes`。加载存档时不会重新抽取实例属性。

## 固定双属性

以下数组均按 `t1/t2/t3/t4/t5` 排列，并直接表示面板值。气血 4 点、法力 2 点、其他属性 1 点各计为一个等价属性点。

| 原型或模板 | 主属性 | 特色副属性 |
| --- | --- | --- |
| 玄金剑 | 金行 `[8,15,21,28,34]` | 攻击 `[3,7,11,14,16]` |
| 青木杖 | 木行 `[8,15,21,28,34]` | 气血 `[12,28,44,56,64]` |
| 镇岳拳套 | 土行 `[8,15,21,28,34]` | 防御 `[3,7,11,14,16]` |
| 沧水符笔 | 水行 `[8,15,21,28,34]` | 法力 `[6,14,22,28,32]` |
| 赤焰法环 | 火行 `[8,15,21,28,34]` | 攻击 `[3,7,11,14,16]` |
| 聚灵冠 | 法力 `[8,14,22,26,30]` | 防御 `[2,4,5,7,8]` |
| 镇元法衣 | 防御 `[5,10,13,17,20]` | 气血 `[12,24,36,48,56]` |
| 行脉胫甲 | 气血 `[16,32,44,60,68]` | 防御 `[3,6,8,10,11]` |
| 锻骨护手 | 根骨 `[3,7,9,12,14]` | 攻击 `[2,4,6,8,9]` |
| 青木佩 | 木行 `[4,7,11,13,14]` | 气血 `[8,16,20,28,32]` |
| 赤焰珠 | 火行 `[4,7,11,13,14]` | 攻击 `[2,4,5,7,8]` |
| 厚土印 | 土行 `[4,7,11,13,14]` | 防御 `[2,4,5,7,8]` |
| 玄金令 | 金行 `[4,7,11,13,14]` | 攻击 `[2,4,5,7,8]` |
| 沧水环 | 水行 `[4,7,11,13,14]` | 法力 `[4,8,10,14,16]` |

## 实例随机属性

- t1 至 t5 分别追加 1/2/3/4/5 项随机属性，因此最终基础属性总数为 3/4/5/6/7。
- 随机池包含攻击、防御、气血、法力、根骨和五行十种属性。
- 随机属性不与固定双属性或其他随机属性重复，保存顺序即抽取顺序。
- 所有核心模板的随机层共享预算 `[1,2,4,6,10]`。整除余数按保存顺序分给前几项，每项至少获得一个等价属性点。
- 随机属性数量只改变分配宽度，不改变该层总预算。
- 相同 RNG 状态会得到相同原型、随机属性和数值。实例生成后以存档字段为准，不按模板重新抽取。

## 七件预算与强度

七件满装包含武器、头盔、护甲、胫甲、护手和两个饰品。以敌人阶位中段等级 `10/30/50/70/90` 为基准，未强化装备约占角色总等价属性 35%，全部强化到当阶上限后约占 55%。

| 部位 | t1 | t2 | t3 | t4 | t5 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 武器 | 12 | 24 | 36 | 48 | 60 |
| 头盔 | 7 | 13 | 20 | 26 | 33 |
| 护甲 | 9 | 18 | 26 | 35 | 44 |
| 胫甲 | 8 | 16 | 23 | 31 | 38 |
| 护手 | 6 | 13 | 19 | 26 | 33 |
| 单个饰品 | 7 | 13 | 20 | 26 | 32 |
| 七件合计 | 56 | 110 | 164 | 218 | 272 |
| 满强化额外点数 | 70 | 140 | 210 | 280 | 350 |

## 强化、词条与升阶

各阶累计强化点上限为 `10/20/30/40/50`。每点消耗强化石 x1，可选择实例 `base_attributes` 中任一属性；气血每点 +4，法力每点 +2，其他属性每点 +1。

各阶默认生成权重为 `55/28/12/4/1`。这些权重、强化上限、强化成本、强化属性单位、词条数和升阶成本均在每件装备自身的 `tiers` / `attribute_units` 中配置；代码只负责解析和执行。

阶位词条槽数为 `1/2/3/3/3`，单件装备最多三个随机战斗词条。洗练通过索引替换一个词条槽，第 N 次洗练消耗 N 张洗练符。

`T -> T+1` 消耗矿石 `8T` 和升阶石 `2T`。升阶保留五行原型、已有随机属性及顺序、强化分配、词条、洗练次数、穿戴者和来源；补抽到新阶位要求的随机属性数量，再按新阶固定值和随机预算重算 `base_attributes`。五阶不可继续升阶。

## 经济与战斗

- Boss 每次死亡必掉与自身数字阶位相同数量的升阶石。
- 分解装备获得 `T + floor(已分配强化点 / 2)` 个强化石，不返还矿石；已装备物品不能分解。
- 装备图纸已退出掉落、保底、坊市和炼器解锁。
- 装备词条影响普通攻击和直接技能伤害，不影响 DOT、反伤或 Mod 自定义伤害。
- 直接伤害依次结算直接伤害加成、暴击、破防与防御、五行克制、直接减伤、护盾和吸血。

## 存档迁移

当前存档版本为 Schema 23，新生成核心装备的 `attribute_generation_version` 为 4。Schema 22 只新增统一道具 Buff 与自动道具栏，不重算 Schema 21 装备或推进装备 RNG；Schema 23 仅移除炼丹学习机制，同样不改动任何装备数据。

- Schema 21 按原阶位、五行原型和已保存的随机属性顺序重算核心装备基础值，不重新抽取且不消耗 RNG。空随机属性列表继续为空。
- Schema 21 保留强化分配、强化总数、词条、洗练、穿戴者、实例 ID 和来源；Mod 与非核心装备不参与重算。

- Schema 20 将五个旧武器 ID 映射到 `weapon`，五个旧饰品 ID 映射到 `accessory`，并保存对应五行原型、名称和图标。
- Schema 20 迁移时旧装备已有 `base_attributes` 原样固化，缺失时按旧模板和阶位确定性还原；之后由 Schema 21 统一切换到当前数值。
- 旧装备的 `rolled_attribute_stats` 初始化为空，之后只有升阶会补抽新属性。
- 强化分配、强化总数、词条、洗练、穿戴者、实例 ID 和来源保持不变。
- Schema 19 的木杖、土拳套和水符笔攻击强化映射仍先执行，再进行 Schema 20 模板收敛。
- Schema 18 的图纸补偿、稳定词条和旧单模板兼容继续保留。

## Mod API 2

核心装备统一挂载只声明导出字段的 `EquipmentConfigResource`，因此身份、显示信息、属性生成和五阶成长均可在 Godot Inspector 中手动编辑。具体装备数值只保存在各自 `.tres`，共享脚本不保存任何装备常量。Mod 装备继续兼容固定模板格式；`EquipmentTemplate` 可选提供 `attribute_variants`、`random_attribute_pool`、`tier_random_attribute_counts` 和 `tier_random_attribute_budgets`，随机配置为空时仍直接使用 `tier_base_attributes`。本版本不提升 Mod API，也不开放 Mod 自定义战斗词条类型。

## 配置格式

`index.tres` 的 `metadata/equipment_index` 只保存十四件装备的路径、六槽位权重、候选装备和旧 ID 别名。每件装备 `.tres` 通过统一导出字段自包含身份、图标、双固定属性顺序、随机池、强化单位和五阶 `tiers`；`EquipmentConfigResource` 只负责 Inspector 字段声明和转换，不参与数值计算，核心资源也不挂载旧 `EquipmentTemplate`。

解析器按路径缓存配置并向调用方返回深拷贝。核心资源缺少五阶、属性重复、单位或成本非法、索引引用失效时不会注册，并输出包含路径与字段的错误；核心配置没有代码默认数值回退。

## 维护与验收

- 十四个核心资源必须使用统一 `EquipmentConfigResource` 导出格式并各自完整配置 t1 至 t5；索引只记录路径和抽取关系，不保存成长数值。
- 固定双属性与随机属性不得重复，最终基础属性顺序必须稳定。
- 新增词条必须同步更新定义、聚合、结算、详情显示和测试。
- Schema 18、19 和 20 迁移必须保持确定性，不得消耗主存档 RNG。
