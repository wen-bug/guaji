{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://guaji.example/schemas/mod/v2/manifest.schema.json",
  "title": "Guaji Mod Manifest v2",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "id",
    "name",
    "version",
    "mod_api_version",
    "game_version",
    "content"
  ],
  "properties": {
    "schema_version": {
      "const": 2
    },
    "id": {
      "type": "string",
      "pattern": "^[a-z0-9][a-z0-9._-]{2,63}$"
    },
    "name": {
      "type": "string",
      "minLength": 1
    },
    "version": {
      "type": "string",
      "pattern": "^(0|[1-9][0-9]*)\\.(0|[1-9][0-9]*)\\.(0|[1-9][0-9]*)(?:-([0-9A-Za-z-]+(?:\\.[0-9A-Za-z-]+)*))?$"
    },
    "description": {
      "type": "string"
    },
    "authors": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      }
    },
    "mod_api_version": {
      "const": 2
    },
    "game_version": {
      "$ref": "#/$defs/versionRange"
    },
    "dependencies": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/dependency"
      }
    },
    "conflicts": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "load_after": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "content": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^res://mods/[^/]+/.+\\.json$"
      }
    },
    "entry_script": {
      "type": "string",
      "pattern": "^(|res://mods/[^/]+/.+\\.gd)$"
    },
    "overrides": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^[a-z_]+:.+$"
      }
    }
  },
  "$defs": {
    "versionRange": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "min": {
          "$ref": "#/$defs/semver"
        },
        "max_exclusive": {
          "$ref": "#/$defs/semver"
        }
      }
    },
    "dependency": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id"
      ],
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^[a-z0-9][a-z0-9._-]{2,63}$"
        },
        "min_version": {
          "$ref": "#/$defs/semver"
        },
        "max_version_exclusive": {
          "$ref": "#/$defs/semver"
        },
        "optional": {
          "type": "boolean",
          "default": false
        }
      }
    },
    "semver": {
      "type": "string",
      "pattern": "^(0|[1-9][0-9]*)\\.(0|[1-9][0-9]*)\\.(0|[1-9][0-9]*)(?:-([0-9A-Za-z-]+(?:\\.[0-9A-Za-z-]+)*))?$"
    }
  }
}
